# Allure results directory (under target so 'mvn clean' removes it)
allure.results.directory=target/allure-results
# Optional: link patterns for issues/tms (uncomment and set your base URL if needed)
# allure.link.issue.pattern=https://jira.example.com/browse/{}
# allure.link.tms.pattern=https://testmanagement.example.com/testcase/{}
