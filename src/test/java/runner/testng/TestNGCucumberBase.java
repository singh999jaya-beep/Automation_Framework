package runner.testng;

import io.cucumber.testng.AbstractTestNGCucumberTests;

/**
 * Base class for TestNG Cucumber runners. Subclasses define feature paths via {@code @CucumberOptions}.
 */
public abstract class TestNGCucumberBase extends AbstractTestNGCucumberTests {
}
